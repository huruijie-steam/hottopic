# 测试环境配置:内存数据库 + 测试 AI key,不执行 MySQL 版 schema.sql
spring:
  datasource:
    url: jdbc:h2:mem:hottopic;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  sql:
    init:
      mode: never
  ai:
    deepseek:
      api-key: test-key
